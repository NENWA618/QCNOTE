# Keep host-built artifacts and secrets out of the build context
# (COPY . . would otherwise overwrite the image's Linux node_modules with the host's)
**/node_modules
.next
.git
.github
.husky
.claude
test-results
coverage
tsconfig.tsbuildinfo
.env
.env.*
!.env.example
*.log
